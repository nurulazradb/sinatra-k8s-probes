# Gemfile
source 'https://rubygems.org'

gem 'sinatra'
gem 'activerecord'
gem 'sqlite3' # Or 'pg' if you prefer PostgreSQL
gem 'json'    # For JSON responses
gem 'puma'    # For HTTP server
gem 'rackup'  # For Rack-based applications
